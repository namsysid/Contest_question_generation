# Historical GPT-5.1 Disease Detectives bank

This directory preserves the complete generated and validation artifacts from the earlier GPT-5.1 run. These files
are historical and are not the canonical production bank. The canonical GPT-5-mini bank is under
`../../generated/gpt-5-mini/`, with its audit trail under `../../validation/gpt-5-mini/`.
